plan:
  name: Example scenario
  owner: your name
  stages:
  - name: stage-one
    trigger_type: delta
    trigger_args:
      seconds: 1
    steps:
    - name: step1
      is_init: true
      attack_module: mod_cmd
      attack_module_args:
          cmd: echo ls
      next:
      - type: result
        value: OK
        step: step2
    - name: step2
      attack_module: mod_cmd
      attack_module_args:
          cmd: $step1.cmd_output
